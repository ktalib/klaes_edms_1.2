; Upload limits for PHP-FPM / FastCGI hosts (ignored under mod_php, which uses .htaccess).
; Must be comfortably above the app's per-file cap (5 MB) and allow several files per request.
upload_max_filesize = 50M
post_max_size = 60M
max_execution_time = 600
max_input_time = 600
memory_limit = 512M
